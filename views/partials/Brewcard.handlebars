
{{#each Brew}}
<div class="card">
    <div class="card-body">
        <!-- try to get picture to sit to the left -->
        <!-- <img src="" alt=""> -->
        <h1 class="card-header">Beer Name goes here {{name}}</h1>
        <h2 class="card-text">Brewery name:{{breweryName}}</h2>
        <h2 class="card-text">Description of beer goes here{{description}}</h2>
        <h3 class="card-text">Alc%{{alcContent}}</h3>
        <h3 class="card-text">type of beer here {{style}}</h3>
    </div>
</div>
{{/each}}